<Window x:Class="DPSpecial.Tools.ECP.ECPZoneManage.view.ECPZoneManageView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DPSpecial.Tools.ECP.ECPZoneManage.view"
        mc:Ignorable="d"
        Title="ECP Zone Manage"
        WindowStartupLocation="CenterScreen"
        SizeToContent="Height"
        MinHeight="150"
        Width="500">
    <Window.Resources>
        <Style TargetType="Button">
            <Setter Property="Height" Value="25"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="Cursor" Value="Hand"/>
        </Style>
    </Window.Resources>
    <StackPanel Margin="15">
        <DataGrid x:Name="ZoneDataGrid"
                  ItemsSource="{Binding Zones}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False"
                  CanUserDeleteRows="False"
                  CanUserSortColumns="False"
                  SelectionMode="Single"
                  ColumnWidth="*"
                  Height="350">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="No." Width="50" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Id}" VerticalAlignment="Center" HorizontalAlignment="Center"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Name">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged, Delay=100}"
                                     VerticalContentAlignment="Center"
                                     BorderThickness="0"
                                     Background="Transparent"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTemplateColumn Header="Color" Width="60" IsReadOnly="True">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <!-- Bind through the DataGrid's own DataContext (the VM) since the row's DataContext is the zone itself. -->
                            <Button BorderThickness="1"
                                    Height="22"
                                    Width="40"
                                    Background="{Binding Color}"
                                    Command="{Binding ElementName=ZoneDataGrid, Path=DataContext.PickColorCommand}"
                                    CommandParameter="{Binding}"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <Grid Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="0" Orientation="Horizontal" HorizontalAlignment="Left">
                <Button Content="Create" Command="{Binding CreateZoneCommand}"/>
                <Button Content="Delete" Command="{Binding DeleteZoneCommand}" Margin="5,0,0,0"/>
            </StackPanel>
            <StackPanel Grid.Column="1" Orientation="Horizontal" HorizontalAlignment="Right">
                <Button Content="Ok" Command="{Binding OkCommand}"/>
                <Button Content="Cancel" Command="{Binding CancelCommand}" Margin="5,0,0,0"/>
            </StackPanel>
        </Grid>
    </StackPanel>
</Window>
